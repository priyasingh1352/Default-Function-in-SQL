# Default-Function-in-SQL
Default Function is the function which sets the default values in column of SQL database.This project demonstrates the use of the DEFAULT constraint in MySQL using a simple student database.
